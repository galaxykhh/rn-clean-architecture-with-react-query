import React from 'react';
import MainNavigation from '@presentation/navigations/MainNavigation';

const NyAppName: React.FC = (props) => {
    return (
        <MainNavigation />
    );
};

export default NyAppName;
